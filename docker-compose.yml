services:
  server:
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      - DB_PATH=/data/secret_share.redb
    volumes:
      - db_data:/data
    ports:
      - "8080:8080"
volumes:
  db_data:
